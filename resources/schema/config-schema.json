{
  "$schema": "https://json-schema.org/draft/2020-12/schema",

  "$id": "https://scm.dev/schemas/config.json",
  "title": "SCM Configuration",
  "description": "Schema for SCM config.yaml files",
  "type": "object",
  "properties": {
    "version": {
      "type": ["string", "number"],
      "description": "Schema version for the config format",
      "examples": ["1.0", 1.0]
    },
    "lm": {
      "type": "object",
      "description": "Language model plugin configuration",
      "properties": {
        "plugin_paths": {
          "type": "array",
          "description": "Paths to search for external plugin binaries",
          "items": {
            "type": "string"
          },
          "examples": [["~/.scm/plugins", ".scm/plugins"]]
        },
        "plugins": {
          "type": "object",
          "description": "Plugin configurations keyed by name",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "default": {
                "type": "boolean",
                "description": "If true, this is the default plugin"
              },
              "model": {
                "type": "string",
                "description": "Default model to use with this plugin",
                "examples": ["opus", "sonnet", "gpt-4"]
              },
              "binary_path": {
                "type": "string",
                "description": "Path to the plugin binary (optional, uses PATH if empty)"
              },
              "args": {
                "type": "array",
                "description": "Command line arguments to pass to the plugin",
                "items": {
                  "type": "string"
                }
              },
              "env": {
                "type": "object",
                "description": "Environment variables to set for the plugin",
                "additionalProperties": {
                  "type": "string"
                }
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "editor": {
      "type": "object",
      "description": "Editor configuration for fragment/prompt editing",
      "properties": {
        "command": {
          "type": "string",
          "description": "Editor command to use",
          "examples": ["vim", "nano", "code"]
        },
        "args": {
          "type": "array",
          "description": "Additional arguments for the editor",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "defaults": {
      "type": "object",
      "description": "Default settings",
      "properties": {
        "profile": {
          "type": "string",
          "description": "Default profile to load when no profile is specified"
        },
        "generators": {
          "type": "array",
          "description": "Generators always run",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "use_distilled": {
          "type": "boolean",
          "description": "Whether to prefer distilled versions of fragments/prompts (default: true)"
        }
      },
      "additionalProperties": false
    },
    "generators": {
      "type": "object",
      "description": "Context generators that produce dynamic content",
      "additionalProperties": {
        "type": "object",
        "required": ["command"],
        "properties": {
          "description": {
            "type": "string",
            "description": "Human-readable description of what this generator produces"
          },
          "command": {
            "type": "string",
            "description": "Command to execute (can be a path or binary name in PATH)",
            "minLength": 1
          },
          "args": {
            "type": "array",
            "description": "Additional arguments for the generator command",
            "items": {
              "type": "string"
            }
          }
        },
        "additionalProperties": false
      }
    },
    "hooks": {
      "$ref": "#/$defs/hooksConfig",
      "description": "Hooks configuration applied globally"
    },
    "profiles": {
      "type": "object",
      "description": "Named collections of context fragments, variables, and generators",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "default": {
            "type": "boolean",
            "description": "Whether this profile is a default profile (automatically loaded when no explicit fragments/tags/profile specified)"
          },
          "description": {
            "type": "string",
            "description": "Human-readable description of this profile"
          },
          "parents": {
            "type": "array",
            "description": "Parent profiles to inherit from (processed depth-first)",
            "items": {
              "type": "string",
              "minLength": 1
            },
            "uniqueItems": true
          },
          "tags": {
            "type": "array",
            "description": "Fragment tags to include (fragments with matching tags are added)",
            "items": {
              "type": "string",
              "minLength": 1
            },
            "uniqueItems": true
          },
          "fragments": {
            "type": "array",
            "description": "Explicit fragment paths to include",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "variables": {
            "type": "object",
            "description": "Variable values for template substitution in fragments",
            "additionalProperties": {
              "type": "string"
            }
          },
          "generators": {
            "type": "array",
            "description": "Generators to run for this profile",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "hooks": {
            "$ref": "#/$defs/hooksConfig",
            "description": "Hooks configuration for this profile (inherited from parents)"
          }
        },
        "additionalProperties": false
      }
    }
  },
  "$defs": {
    "hook": {
      "type": "object",
      "description": "A hook definition",
      "properties": {
        "matcher": {
          "type": "string",
          "description": "Regex pattern to filter when hook fires (by tool name, event source, etc.)"
        },
        "command": {
          "type": "string",
          "description": "Shell command to execute",
          "minLength": 1
        },
        "type": {
          "type": "string",
          "description": "Hook handler type",
          "enum": ["command", "prompt", "agent"],
          "default": "command"
        },
        "prompt": {
          "type": "string",
          "description": "Prompt text for prompt/agent hook types"
        },
        "timeout": {
          "type": "integer",
          "description": "Execution timeout in seconds",
          "minimum": 1,
          "default": 30
        },
        "async": {
          "type": "boolean",
          "description": "Run hook in background (command hooks only)",
          "default": false
        }
      },
      "additionalProperties": true
    },
    "hookArray": {
      "type": "array",
      "description": "Array of hooks for an event",
      "items": {
        "$ref": "#/$defs/hook"
      }
    },
    "unifiedHooks": {
      "type": "object",
      "description": "Unified hook events (translated to backend-specific format)",
      "properties": {
        "pre_tool": {
          "$ref": "#/$defs/hookArray",
          "description": "Before tool execution"
        },
        "post_tool": {
          "$ref": "#/$defs/hookArray",
          "description": "After tool execution"
        },
        "session_start": {
          "$ref": "#/$defs/hookArray",
          "description": "When session starts"
        },
        "session_end": {
          "$ref": "#/$defs/hookArray",
          "description": "When session ends"
        },
        "pre_shell": {
          "$ref": "#/$defs/hookArray",
          "description": "Before shell command execution"
        },
        "post_file_edit": {
          "$ref": "#/$defs/hookArray",
          "description": "After file edit"
        }
      },
      "additionalProperties": false
    },
    "hooksConfig": {
      "type": "object",
      "description": "Hooks configuration",
      "properties": {
        "unified": {
          "$ref": "#/$defs/unifiedHooks",
          "description": "Unified hooks (translated per-backend)"
        },
        "plugins": {
          "type": "object",
          "description": "Backend-specific passthrough hooks (keyed by plugin name)",
          "additionalProperties": {
            "type": "object",
            "description": "Backend-native hook events",
            "additionalProperties": {
              "$ref": "#/$defs/hookArray"
            }
          }
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
