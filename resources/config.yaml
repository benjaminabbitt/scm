lm:
  plugins:
    claude-code:
      default: true

editor:
  command: nano

generators:
  simple:
    description: Wrapper that runs any command and outputs as fragment
    command: scm-gen-simple
  git:
    description: Provides git repository context (branch, status, recent commits)
    command: scm-gen-git

